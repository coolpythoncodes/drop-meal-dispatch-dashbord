import '../Sass/Search.scss'

const Search = () => {
    return (
        <form className="search">
            <input type="text" placeholder='Search' />
        </form>
    );
}

export default Search;
